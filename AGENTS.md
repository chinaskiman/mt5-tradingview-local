# Codex Project Instructions

Always read `PROJECT_LOG.md` before making changes.

After every meaningful change, update `PROJECT_LOG.md`.

Keep notes concise, practical, and useful for debugging.

Do not store secrets or credentials.

Prefer appending new history entries instead of deleting old ones.